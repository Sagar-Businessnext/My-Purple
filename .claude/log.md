
## 2026-06-05T12:09:37Z — Milestone: first run + hardening
- Action: built live self-check, hardened startup/health, one-command setup, first-run guide (bnac python workflow).
- Files: src/purple/selfcheck.py (new), src/purple/app.py (/health + startup summary), setup.ps1 (new), docs/FIRSTRUN.md (new), pyproject.toml (+purple-selfcheck script), README.md, tests/unit/test_selfcheck.py (new).
- Result: ruff clean; pytest 39/40 pass in sandbox (1 = py3.10-only datetime.UTC, passes on py3.12 target).

## 2026-06-08T05:57:55Z — Milestone: Vision (Purple's eyes)
- Action: added vision layer via local VLM (Qwen3-VL) — VisionProvider + see_screen / find_on_screen / look_and_click (commit-guarded), feeding coords into desktop_ui._click_at. Default model qwen3-vl:8b (config PURPLE_VISION_MODEL).
- Files: src/purple/vision/{__init__,provider}.py (new), src/purple/tools/vision.py (new), src/purple/config.py (+vision_model), src/purple/tools/__init__.py (load vision), tests/unit/test_vision.py (new).
- Result: ruff clean; pytest 43/44 (1 = py3.10 datetime.UTC, OK on 3.12 target). Next: Live Desktop UI.

## 2026-06-08T06:09:36Z — Milestone: Live Desktop UI
- Action: /config GET+POST (config_api.py, .env persistence, secret masking, restart flags) + live tool-event broadcast (Agent.emit) + /health vision_model; React UI rebuilt with Chat/Monitor/Settings tabs (ui-src App.tsx, purple.ts, App.css).
- Files: src/purple/config_api.py (new), src/purple/agent/orchestrator.py (emit), src/purple/app.py (/config + agent emit + health), frontend/ui-src/{App.tsx,purple.ts,App.css}, tests/unit/test_config_api.py (new).
- Result: ruff clean; pytest 46/47 (1 = py3.10 datetime.UTC). Next: Observability.

## 2026-06-08T06:36:42Z — Milestone: Observability
- Action: observability.py (Prometheus metrics facade + optional OTel tracing, graceful no-op); rotating file logs (utils/logging.py via stdlib root); instrumented registry.execute (tool metrics+span), ollama_client.chat (llm metrics+span), agent.respond (turn counter); GET /metrics + request counter + setup_tracing on startup; config observability fields; docs/OBSERVABILITY.md; deps prometheus-client (core) + opentelemetry (extra).
- Files: src/purple/observability.py (new), utils/logging.py, config.py, tools/registry.py, llm/ollama_client.py, agent/orchestrator.py, app.py, pyproject.toml, docs/OBSERVABILITY.md, tests/unit/test_observability.py (new).
- Result: ruff clean; pytest 49/50 (1 = py3.10 datetime.UTC). Next: email/calendar (final milestone).

## 2026-06-08T06:44:38Z — Milestone: Observability surfaced in the Live Desktop UI
- Action: observability.metrics_summary() (JSON metrics; fixed _avg_ms to sum across label series) + GET /metrics/summary + GET /logs (tail purple.log); Monitor tab now shows metric cards (turns, llm/tool counts + avg latency), per-tool usage, and a recent-logs view alongside health + live feed.
- Files: src/purple/observability.py, src/purple/app.py, frontend/ui-src/{App.tsx,purple.ts,App.css}, tests/unit/test_observability.py.
- Result: metrics_summary verified in isolation (counts/avg correct after bug fix); ruff clean.

## 2026-06-08T07:12:20Z — Milestone: Email + Calendar (Google) — final roadmap milestone
- Action: Google (Gmail+Calendar) integration behind a provider (integrations/google.py, OAuth token local) + one-time auth CLI (integrations/google_auth.py); tools list_emails/search_email/read_email/draft_email (prepare) + send_email/create_event (commit-guarded); config google paths; [google] dep extra; docs/EMAIL_CALENDAR.md.
- Files: src/purple/integrations/{__init__,google,google_auth}.py, src/purple/tools/email_calendar.py, src/purple/config.py, src/purple/tools/__init__.py, pyproject.toml, docs/EMAIL_CALENDAR.md, tests/unit/test_email_calendar.py.
- Result: ruff clean; pytest 52/53 (1 = py3.10 datetime.UTC, OK on py3.12). ALL 4 roadmap milestones complete (vision, desktop UI, observability, email/calendar).

## 2026-06-12T05:46:08Z — Plan: Jarvis roadmap (6 milestones)
- Action: defined + re-prioritized 6 feature milestones toward Jarvis (docs/JARVIS_ROADMAP.md). Order: M1 always-on presence, M2 proactivity/trigger engine, M3 deeper knows-me, M4 real-time duplex voice, M5 long-horizon autonomy, M6 smart home. Presented M1 detailed plan for approval before building. No code yet.

## 2026-06-12T06:17:36Z — Milestone M1: Always-on presence + GPU Focus mode (Jarvis roadmap)
- Action: gpu.py (nvidia-smi util/VRAM + gpu_busy), focus.py (manual + auto-yield should_yield_gpu), service.py (SingleInstance PID lock + run server, purple-service entry), token-auth middleware (X-Purple-Token for non-localhost), /status + /focus endpoints + app start time, config (api_token/focus_mode/auto_focus/gpu thresholds); autostart scripts (install/uninstall Scheduled Task); Monitor UI uptime + GPU badge + Focus toggle; docs/ALWAYS_ON.md.
- Files: src/purple/{gpu,focus,service}.py (new), app.py, config.py, pyproject.toml, scripts/{install,uninstall}_service.ps1, frontend/ui-src/{App.tsx,purple.ts}, docs/ALWAYS_ON.md, tests/unit/test_always_on.py.
- Result: M1 core verified in isolation (gpu parse/busy, focus yield, single-instance lock); ruff clean. Autostart + LAN + real nvidia-smi verified on user's PC. Next milestone: M2 proactivity/trigger engine.

## 2026-06-12T09:41:00Z — Plan: Milestone 2 (proactivity / trigger engine)
- Action: appended M2 detailed plan to docs/JARVIS_ROADMAP.md (watchers -> rules -> actions; NL triggers; autonomy levels; notifications; UI Automations tab; built on M1 always-on + scheduler + email/calendar + event bus + TTS + Focus). Presented for review; no code yet.

## 2026-06-12T11:29:48Z — Milestone M2 slice 1: proactive brain (trigger engine)
- Action: built triggers/ (priority.py Tier-1 classify+breakthrough+quiet-hours, notify.py voice/toast/feed with Focus+breakthrough gating + debounce, watchers.py system/email/calendar, engine.py APScheduler tick); config triggers fields; wired engine into service lifespan; win11toast dep.
- Files: src/purple/triggers/{__init__,priority,notify,watchers,engine}.py (new), config.py, app.py, pyproject.toml, tests/unit/test_triggers.py (new).
- Result: 7 tests pass (classify, spam, quiet-hours, breakthrough, voice-suppressed-while-gaming, breakthrough+debounce, engine dispatch); ruff clean.
- NEXT (M2 slice 2): Kokoro voice (remove Piper, XTTS backup); phone/news/weather/downloads/system-health/active-window watchers; user rules + NL triggers; Automations UI; autonomy enforcement.

## 2026-06-12T11:55:00Z — M2 slice 2a: natural voice (Kokoro) + phone watchers
- Context read: ~/.claude/CLAUDE.md, D:\Purple\.claude\CLAUDE.md, JARVIS_ROADMAP.md (M2), memory/MEMORY.md.
- Action (voice): replaced Piper with Kokoro (kokoro-onnx, af_bella, CPU-light) as the default natural TTS; kept Coqui XTTS-v2 as an optional clone backup. local_provider.py rewritten (faster-whisper STT kept; _to_wav float32->int16 WAV; _ensure_kokoro/_kokoro_sync; _xtts_sync; engine dispatch by tts_engine). config tts_engine/kokoro_voice/kokoro_speed/xtts_speaker_wav. selfcheck + config_api + .env.example + setup.ps1 (downloads kokoro-v1.0.onnx + voices-v1.0.bin) + pyproject (kokoro-onnx dep, [xtts] extra) updated.
- Action (phone watchers): adb.py +cmd_call_state/cmd_contacts builders, +normalize_number/parse_call_state/parse_contacts parsers. watchers.py +Watcher.interval, +CallWatcher (known contact -> IMPORTANT breakthrough one gentle spoken nudge; unknown number -> NORMAL/quiet, treated as likely spam; contacts cached over ADB), +MessageWatcher (ADB notification dump -> Tier-1 classify). engine.py groups watchers by poll interval (calls poll ~5s), registers phone watchers gated by enable_phone_watchers. config enable_phone_watchers/phone_poll_seconds. config_api exposes enable_triggers + enable_phone_watchers (UI toggles, restart-required).
- Files: src/purple/speech/local_provider.py, src/purple/config.py, src/purple/config_api.py, src/purple/selfcheck.py, src/purple/phone/adb.py, src/purple/triggers/watchers.py, src/purple/triggers/engine.py, pyproject.toml, .env.example, setup.ps1, tests/unit/test_phone_watchers.py (new), tests/unit/test_speech_tts.py (new).
- Result: ruff clean; 18/18 unit tests pass (7 triggers + 8 phone watchers + 3 TTS) in isolated /tmp tree (mount served truncated copies — authoritative files on disk are correct). Verified via py_compile of full import graph.
- NEXT (M2 slice 2b): news/weather/downloads-finished/system-health/active-window watchers; user-defined rules (Rule DB CRUD) + natural-language trigger tool; Automations UI tab; autonomy enforcement (notify/confirm/act) in engine.

## 2026-06-12T12:40:00Z — M2 slice 2b: user-defined automation rules (engine + persistence + NL tools + API)
- Context read: ~/.claude/CLAUDE.md, D:\Purple\.claude\CLAUDE.md, JARVIS_ROADMAP.md (M2), memory/MEMORY.md + purple-build-plan.md.
- Action: added a user-rules layer on top of the watchers so the user shapes notifications without code (per "automations controlled from UI"). triggers/rules.py (pure): Rule(name/keywords/source/min_priority/effect/enabled), rule_matches, apply_rules (speak beats mute), shape_event (mute->drop, speak->escalate to IMPORTANT so it breaks through). Persistence: AutomationRule ORM model (memory/models.py) + Memory.add_rule/list_rules_raw/get_rules/set_rule_enabled/delete_rule (store.py, ORM<->domain converters). Engine: holds self.rules, applies shape_event per event in tick(), load_rules() from DB + module-level _rules_dirty flag + request_rules_reload() (edits apply next tick, no per-tick DB hit). NL tools (tools/automations.py): add/list/toggle/remove_automation (validates effect + priority; safe = notification-only, no confirmation) registered in load_tools. API (app.py): GET/POST/PATCH/DELETE /automations for the UI, each mutation calls request_rules_reload.
- Files: src/purple/triggers/rules.py (new), src/purple/triggers/engine.py, src/purple/memory/models.py, src/purple/memory/store.py, src/purple/tools/automations.py (new), src/purple/tools/__init__.py, src/purple/app.py, tests/unit/test_rules.py (new), tests/unit/test_automations_tool.py (new).
- Result: ruff clean; 29/29 targeted tests pass (8 rules+engine, 6 automation-tool, 8 phone, 3 TTS, 4 prior triggers) in isolated /tmp tree (mount served truncated copies again — authoritative on-disk files are correct; reconstructed config/watchers/engine/adb/local_provider/store/models to verify).
- NEXT (M2 slice 2c): React Automations tab (frontend/ui-src) bound to /automations; then news/weather/downloads-finished/system-health/active-window watchers; then autonomy 'act' (rule-triggered tool runs under confirm/act + safety). Then M3 deeper-memory.

## 2026-06-12T13:20:00Z — M2 slice 2c: Automations UI tab (the user-facing control surface)
- Context read: ~/.claude/CLAUDE.md, project CLAUDE.md, JARVIS_ROADMAP.md (M2), memory.
- Action: added the Automations tab to the desktop UI, bound to the /automations API from slice 2b — the "controlled from UI only" surface the user asked for. purple.ts: Automation type + listAutomations/addAutomation/toggleAutomation/removeAutomation client methods. App.tsx: Tab union + tabs array gain "automations"; new AutomationsPanel (add-form: name / keywords / source / min_priority / effect dropdowns; live rule list with effect+source+priority+keyword badges; per-rule On/Off toggle + Delete; reloads after each change). App.css: .addform / .rules / .rule / effect badge styles.
- Files: frontend/ui-src/purple.ts, frontend/ui-src/App.tsx, frontend/ui-src/App.css.
- Result: esbuild parses purple.ts (160 lines), App.tsx (465 lines) and bundles App.css (79 lines) with zero errors/warnings (reconstructed authoritative copies in /tmp — mount served truncated versions again). No Python changes this slice.
- NEXT (M2 remaining): news/weather/downloads-finished/system-health/active-window watchers; rule-triggered tool ACTIONS with autonomy (notify/confirm/act) under the commit-confirm safety layer (this is what the unused `autonomy` setting is for). Then M3 deeper-memory.

## 2026-06-12T14:00:00Z — M2 slice 2d: downloads + system-health watchers
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md (M2), memory.
- Action: added two local watchers. DownloadsWatcher (source "download"): watches Downloads folder (config download_dir, blank=~/Downloads), surfaces newly-appeared complete files, ignores .crdownload/.part/.partial/.tmp/.download, primes baseline on first run. SystemHealthWatcher (source "system"): psutil cpu_percent + virtual_memory; fires only after 3 consecutive breaches with hysteresis (won't nag until usage recovers) — CPU high = NORMAL, RAM pressure = IMPORTANT (16GB box). Both wired into engine _default_watchers. config: download_dir, cpu_high_pct(90), ram_high_pct(90). config_api EDITABLE gains download_dir/cpu_high_pct/ram_high_pct. (News/weather intentionally deferred to the daily briefing rather than interrupt-style watchers.)
- Files: src/purple/triggers/watchers.py, src/purple/triggers/engine.py, src/purple/config.py, src/purple/config_api.py, tests/unit/test_watchers_extra.py (new).
- Result: ruff clean; 33/33 targeted tests pass (4 new watcher tests: downloads prime+notify, missing-dir, CPU sustained+hysteresis, RAM important+recover/re-arm; + 29 prior) in isolated /tmp tree (mount truncated copies again — reconstructed config/watchers/engine/adb/local_provider/automations/tools-init/config_api to verify).
- NEXT (M2 finale): rule-triggered tool ACTIONS with autonomy enforcement (notify/confirm/act) under the commit-confirm safety layer — uses the so-far-unused config.autonomy. Then M3 deeper-memory.

## 2026-06-12T14:45:00Z — M2 slice 2e: GPU health + rule-triggered actions (M2 COMPLETE)
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md (M2), memory.
- Action (GPU health): gpu.py +_query_total() + status() now returns vram_total_mb/vram_pct (parse()/gpu_busy() untouched so M1 tests stay green). SystemHealthWatcher refactored to a _track(key,value,threshold) hysteresis helper and now also checks GPU util (NORMAL) + GPU VRAM (IMPORTANT) when a GPU is present. config gpu_high_pct(97)/gpu_vram_high_pct(92), UI-editable.
- Action (rule actions + autonomy): Rule +action/action_args; apply_rules collects (tool,args) from matched rules; engine._run_action enforces settings.autonomy — notify=announce only, confirm=surface an approve-in-chat prompt, act=run via registry.execute with a deny-risky approver so requires_confirmation tools NEVER run unattended (safe tools run). Model +action/action_args columns; store add_rule/converters persist them (JSON). add_automation tool + /automations POST + Automations UI add-form gain an optional "run a tool" field; rule cards show a ▶action badge; hint points at the autonomy setting.
- Files: src/purple/gpu.py, src/purple/config.py, src/purple/config_api.py, src/purple/triggers/{watchers,engine,rules}.py, src/purple/memory/{models,store}.py, src/purple/tools/automations.py, src/purple/app.py, frontend/ui-src/{purple.ts,App.tsx}, tests/unit/{test_watchers_extra,test_rules,test_automations_tool}.py.
- Result: ruff clean; 35/35 targeted Python tests pass (GPU util+vram, no-gpu-silent, apply_rules actions, engine act-runs + notify-doesn't, + prior); esbuild clean on purple.ts/App.tsx/App.css. Verified in isolated /tmp tree (mount truncation again — reconstructed the full triggers+tools+memory+gpu+config import graph).
- *** MILESTONE M2 (proactivity / trigger engine) COMPLETE: watchers (system/health+GPU/downloads/email/calendar/call/message) -> Tier-1 priority + breakthrough -> user rules (mute/speak/notify + run-tool under autonomy) -> notifier (voice/toast/feed) ; Automations UI + NL tools ; Kokoro voice. ***
- NEXT: M3 — deeper memory / "knows me" (richer long-term memory, preferences, context-awareness incl. active-window as a context signal).

## 2026-06-12T15:10:00Z — Plan: Milestone 3 (deeper "knows me" / memory)
- Action: scoped M3 into 4 slices (3a smart memory + profile, 3b document RAG, 3c persona, 3d speaker ID). User decisions: lead with 3a; auto-learning = Balanced; Speaker ID deferred. Appended detailed plan to docs/JARVIS_ROADMAP.md. No code yet — presented 3a plan for go-ahead.

## 2026-06-12T15:40:00Z — M3 slice 3a-1: memory foundation (profile, entities, categories, auto-learning modes)
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md (M3 plan), memory.
- Action: started M3. memory/extract.py (NEW, pure — no DB/LLM): 3 auto-learning modes (moderate/high/aggressive → guideline + max_facts), CATEGORIES, normalize_mode/category, split_aliases, is_duplicate (cosine ≥0.90), profile_summary, extraction_prompt (mode-tuned, excludes secrets/health). Models: ProfileEntry (key/value profile) + Entity (person/project/place/org; relation/aliases/notes) — auto-created by init_db. Store: set_profile/get_profile/profile_summary, add_entity/list_entities/delete_entity, forget_fact, and observe() REWORKED — GPU-gated (skips when focus.should_yield_gpu()), mode-tuned extraction → dedup via _remember_dedup (updates nearest fact when cosine-similar instead of duplicating) → categorized Fact.kind. config auto_memory now default True + auto_memory_mode("moderate"); config_api exposes auto_memory_mode. Tools (memory_tools.py): remember (+category), set_profile, get_profile, remember_person, list_people. orchestrator._memory_context now injects profile summary + relevant facts each turn.
- Files: src/purple/memory/extract.py (new), src/purple/memory/models.py, src/purple/memory/store.py, src/purple/config.py, src/purple/config_api.py, src/purple/agent/orchestrator.py, src/purple/tools/memory_tools.py, tests/unit/test_memory_extract.py (new).
- Result: ruff clean (8 files); 7/7 pure extract tests pass; store/models/config_api py_compile OK (DB code runs on user PC). Verified in isolated /tmp tree (mount truncation as usual).
- NEXT (M3 3a-2): episodic session summaries + rolling-summary injection; consolidation/forget reflection pass; Memory UI tab (view/search/edit/forget facts + profile + people, toggle auto-learn mode) + /memory API. Then 3b document RAG, 3c persona, 3d speaker-ID (required voice access gate).

## 2026-06-12T16:10:00Z — M3 slice 3a-2: Memory API + Memory UI tab
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md (M3), memory.
- Action: store +list_facts(limit,category)/search_facts(semantic)/update_fact + _fact_to_dict. /memory API (app.py): GET /memory (profile+people+facts+auto_memory_mode), GET /memory/search?q, POST/PATCH/DELETE /memory/fact, POST /memory/profile, DELETE /memory/person/{id}. Frontend: purple.ts MemoryFact/Person/MemoryOverview types + memory/memorySearch/addFact/deleteFact/setProfileField/deletePerson; App.tsx 5th tab "memory" + MemoryPanel (auto-learn mode selector wired to /config; editable profile fields; people list w/ forget; fact search + add + forget).
- Files: src/purple/memory/store.py, src/purple/app.py, frontend/ui-src/{purple.ts,App.tsx}.
- Result: esbuild clean (purple.ts + MemoryPanel); store excerpt + /memory routes py_compile OK; ruff clean on store excerpt (route-stub ANN noise only). Verified in isolated /tmp (workspace had reset; rebuilt).
- NEXT (M3 3a-3): episodic session summaries + rolling-summary injection; consolidation/forget reflection pass (periodic merge dups + decay). Then 3b document RAG, 3c persona, 3d speaker-ID (required voice gate).

## 2026-06-12T16:50:00Z — M3 self-awareness slice (delivery-aware voice + self-health + self-state)
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md (self-awareness slice), memory. Prompted by user: "if my speaker's off she talks into the void" — she had no delivery-awareness.
- Action: audio.py (NEW) — output_state() via pycaw/Core Audio (graceful if pycaw missing/non-Windows → assume audible), pure _audible(available,muted,volume), can_be_heard(), describe(). notify.py — pure plan_channels() (voice only if audible; if voice wanted but can't land → toast fallback even in quiet hours); speak() now returns bool; notify() checks audio.can_be_heard() and returns audible in result. selfstate.py (NEW) — voice_issue()/context_note() surfaced to the agent. watchers.py — SelfWatcher (source "self"): edge-triggered warnings when audio out / local LLM / DB go down (warns once per outage, resets on recovery; LLM RuntimeError = not-initialised, no false alarm). engine — SelfWatcher in defaults gated by enable_self_watcher. orchestrator — injects selfstate.context_note() when degraded so Purple can say "you're muted, keeping this on screen". config enable_self_watcher; config_api exposes it (restart-required). pyproject + pycaw/comtypes (win32).
- Files: src/purple/audio.py (new), src/purple/selfstate.py (new), src/purple/triggers/{notify,watchers,engine}.py, src/purple/agent/orchestrator.py, src/purple/config.py, src/purple/config_api.py, pyproject.toml, tests/unit/test_self_awareness.py (new).
- Result: ruff clean (9 files); 9/9 self-awareness tests pass (audible truth table, channel-plan incl. muted→toast fallback + breakthrough-in-quiet override, selfstate note, notify muted-fallback integration, SelfWatcher edge-trigger+recover). Verified isolated /tmp.
- ANSWER to user's Q: before this, NO — a muted speaker doesn't raise, so Purple spoke into the void unaware. Now: she detects it, falls back to toast+screen, proactively warns via SelfWatcher, and can talk about it (self-state in context).
- NEXT: resume M3 3a-3 (episodic session summaries + rolling-summary injection; consolidation/forget reflection pass). Then 3b docs RAG, 3c persona, 3d speaker-ID (required voice gate).

## 2026-06-12T17:30:00Z — M3 slice 3a-3: episodic session summaries + consolidation (3a COMPLETE)
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md (M3), memory.
- Action: SessionSummary model (one rolling summary per session_id, embedded). store: summarize_session (LLM, GPU-gated, upsert), maybe_summarize (every N messages via cheap count), get_session_summary, search_summaries (semantic episodic recall), consolidate (merge near-dup facts by cosine, keep newest wording). extract.py +should_summarize/summarize_prompt/cosine (pure). orchestrator: respond(session_id) threads through; _memory_context injects rolling session summary (current) + top episodic summary (past) + profile + facts. app /chat passes session_id + calls maybe_summarize after each turn. memory_tools +consolidate_memory tool. config session_summarize_every(10).
- Files: src/purple/memory/{models,store,extract}.py, src/purple/agent/orchestrator.py, src/purple/app.py, src/purple/tools/memory_tools.py, src/purple/config.py, tests/unit/test_memory_extract.py.
- Result: ruff clean (store/models/extract/memory_tools/config/orchestrator); pure helpers verified (should_summarize, summarize_prompt, cosine truth tables); store/models/orchestrator py_compile OK. Verified isolated /tmp.
- *** M3 SLICE 3a (smart memory + profile) COMPLETE: categorized facts + structured profile + people/entities + balanced auto-learning modes (dedup-update, GPU-gated) + richer context injection (profile+rolling+episodic+facts) + episodic session summaries + consolidate/forget + Memory UI tab + self-awareness (delivery-aware voice + self-health). ***
- DEFERRED (small follow-ups): weekly auto-consolidation scheduler job; age-based fact decay (config-gated). 
- NEXT: M3 3b document RAG (ingest files/folders → chunk+embed → retrieval). Then 3c persona, 3d speaker-ID (required voice gate). Then M4 duplex voice, M5 autonomy/planner, M6 smart-home.

## 2026-06-12T17:45:00Z — Plan: M3 slice 3b (document RAG)
- Action: scoped 3b into 3b-1 (ingestion: Document/Chunk models, pdf/docx/md/txt parsers, pure chunker, idempotent ingest, mgmt tools, pypdf+python-docx deps) and 3b-2 (retrieval tool + watched knowledge_dir + Knowledge UI). User decisions: tool-based retrieval (cites source); manual + watched-folder ingest; PDF/Word/MD/text. Appended plan to docs/JARVIS_ROADMAP.md. No code yet — presented for go-ahead.

## 2026-06-12T18:20:00Z — M3 slice 3b-1: document ingestion core (RAG)
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md (3b plan), memory.
- Action: documents.py (NEW) — pure chunk_text (overlapping word-windows), is_supported/title_for/file_sha, extract_text (pdf via pypdf / docx via python-docx lazy / md+txt builtin; best-effort, never raises). Models Document(path/title/sha/n_chunks) + Chunk(document_id FK CASCADE, ordinal, text, embedding). store: ingest_file (parse→chunk→embed→store, idempotent by sha, re-chunk on change), ingest_folder (rglob supported, per-file best-effort), list_documents, remove_document, search_documents (cosine over chunks, returns passage+source title; retrieval wiring is 3b-2). tools/documents.py — ingest_documents/list_documents/remove_document (registered). config knowledge_dir (for 3b-2 watched folder). pyproject + pypdf + python-docx.
- Files: src/purple/documents.py (new), src/purple/memory/models.py, src/purple/memory/store.py, src/purple/tools/documents.py (new), src/purple/tools/__init__.py, src/purple/config.py, pyproject.toml, tests/unit/test_documents.py (new).
- Result: ruff clean (documents/tools/models/store-ingest-excerpt/test); 8/8 chunker+parser tests pass (empty/short/long+overlap/coverage, supported+title, txt/md extract, unsupported→"", sha-changes); models + store-excerpt py_compile OK. Verified isolated /tmp (mount truncation as usual). DB tables auto-create via init_db on user's PC.
- NEXT (3b-2): search_documents retrieval TOOL the agent calls (cites source); watched knowledge_dir auto-ingest (APScheduler); Knowledge UI section + /documents API. Then 3c persona, 3d speaker-ID (required voice gate).

## 2026-06-12T19:00:00Z — M3 slice 3b-2: retrieval tool + watched folder + Knowledge UI (3b COMPLETE)
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md (3b), memory.
- Action: search_documents retrieval tool (returns passages+source so she cites). scheduler: periodic (30 min) ingest of settings.knowledge_dir when set (reuses APScheduler, best-effort). /documents API (app.py): GET /documents, POST /documents/ingest (file or folder), GET /documents/search?q, DELETE /documents/{id}. Frontend: purple.ts KbDocument + documents()/ingestDocuments()/removeDocument(); MemoryPanel gains a Knowledge section (learn a file/folder path, list docs with passage count + path, remove).
- Files: src/purple/tools/documents.py, src/purple/scheduler.py, src/purple/app.py, frontend/ui-src/{purple.ts,App.tsx}.
- Result: esbuild clean (purple.ts + Knowledge UI component); scheduler + documents tool py_compile + ruff clean; /documents routes mirror the /memory pattern. Verified isolated /tmp.
- *** M3 SLICE 3b (Document RAG) COMPLETE: ingest (pdf/docx/md/txt, idempotent) → chunk+embed pgvector → search_documents tool (cited) + watched knowledge folder + Knowledge UI. ***
- NEXT: M3 3c persona (configurable personality in system prompt, editable from Settings, applied to chat + spoken nudges). Then 3d speaker-ID (REQUIRED voice gate). Then M4 duplex voice, M5 autonomy/planner, M6 smart-home.

## 2026-06-12T19:10:00Z — Plan: M3 slice 3c (persona)
- Action: scoped 3c (single slice). Decisions: preset + free-text; personalize from profile; default warm & concise. config assistant_name/persona_tone/persona_style/persona_use_profile; prompts.build_system_prompt(profile) composing identity+tone+free-text+profile-address over fixed capabilities+safety; orchestrator builds per-turn from profile; persona fields ride the Settings config form. Appended plan to JARVIS_ROADMAP.md. No code yet.

## 2026-06-12T19:35:00Z — M3 slice 3c: configurable persona
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md (3c), memory.
- Action: prompts.py rewritten — TONE_PRESETS (warm/professional/playful/terse) + fixed CAPABILITIES + non-negotiable SAFETY + build_system_prompt(profile, name/tone/style/use_profile) composing identity over the constants; SYSTEM_PROMPT kept as the default for back-compat. config: assistant_name("Purple"), persona_tone("warm"), persona_style(""), persona_use_profile(True). orchestrator: builds the system prompt per turn from get_profile() (was a static string). config_api EDITABLE += the 4 persona fields → they appear in the Settings tab automatically (live, no restart). Spoken nudges unchanged (literal + af_bella voice = audible persona).
- Files: src/purple/agent/prompts.py, src/purple/config.py, src/purple/agent/orchestrator.py, src/purple/config_api.py, tests/unit/test_persona.py (new).
- Result: ruff clean (prompts/orchestrator); 6/6 persona tests pass (name substitution, tone presets + fallback, free-text appended, profile address line, profile-ignored-when-disabled, safety+capabilities always present); orchestrator compiles. Verified isolated /tmp.
- NEXT: M3 3d — Speaker ID (REQUIRED voice access gate: only enrolled voices may talk to Purple; enroll user voice embedding, recognize owner on the wake/mic path, refuse unrecognized). Finishes M3.

## 2026-06-12T19:50:00Z — Plan: M3 slice 3d (Speaker ID — required voice gate)
- Action: scoped 3d into 3d-1 (speaker.py provider via Resemblyzer + voiceprint storage + config require_enrolled_voice/speaker_threshold + [speaker] extra) and 3d-2 (gate process_command + /voice; /voice/enroll + Settings enroll UI; ignore+UI-note on unknown). Decisions: multiple registered voices; ignore+show in UI; enforce only once enrolled (fail-open until then / if model missing). Gate = voice path only. Appended plan to JARVIS_ROADMAP.md. No code yet.

## 2026-06-12T20:20:00Z — M3 slice 3d-1: speaker-ID provider + enrollment + storage
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md (3d + fail-closed correction), memory.
- Action: purple/speaker.py — voiceprint JSON storage in models_dir (load/save/has_enrollments/list_speakers/remove_speaker); pure _cosine + best_match + _match(threshold); PURE decide_access(enabled, enrolled, model_available, recognized) = FAIL-CLOSED (gate off→allow; not enrolled→allow; enrolled+no model→DENY; enrolled+model→allow iff recognized); Resemblyzer encoder lazy + available() + embed/enroll/identify/is_recognized (optional dep, never imported unless used). config require_enrolled_voice(True)/speaker_threshold(0.75). pyproject [speaker]=resemblyzer extra.
- Files: src/purple/speaker.py (new), src/purple/config.py, pyproject.toml, tests/unit/test_speaker.py (new).
- Result: ruff clean (speaker + config); 6/6 tests pass (decide_access fail-closed truth table incl. enrolled+no-model→deny, _match threshold, _cosine incl shape-mismatch, best_match highest, voiceprint storage roundtrip, missing-file→empty). Verified isolated /tmp.
- NEXT (3d-2, finishes M3): gate VoiceLoop.process_command + /voice with decide_access (ignore + emit "voice not recognized" to feed; fail-closed when enrolled+model broken, loud warn via SelfWatcher/selfcheck); /voice/enroll API + Settings "Enroll your voice" UI (browser mic) + list/remove enrolled voices.

## 2026-06-12T21:00:00Z — M3 slice 3d-2: gate voice path + enroll UI (M3 COMPLETE)
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md (3d + fail-closed), memory.
- Action: speaker.py refactored to DRY _run_gate(get_match) → authorize(samples) + authorize_file(path) + embed_file/enroll_file. VoiceLoop.process_command now gates first via speaker.authorize (off-thread); on deny → emit {voice: not_recognized, reason} and STOP (no transcribe/agent); verifier_unavailable also emits a loud "Voice locked" alert (fail-closed). /voice endpoint gates via authorize_file before transcribe. New endpoints: POST /voice/enroll (name+file → enroll_file), GET /voice/voices (list + available + enforced), DELETE /voice/voices/{name}. Frontend: purple.ts voices/enrollVoice/removeVoice; VoiceEnroll component (browser mic → record 4s → POST; shows locked/enforced + model-missing status; list + remove voices) rendered atop the Settings tab.
- Files: src/purple/speaker.py, src/purple/voice/loop.py, src/purple/app.py, frontend/ui-src/{purple.ts,App.tsx}, tests/unit/test_speaker.py.
- Result: ruff clean (speaker + voice gate excerpt); 6 gate scenarios verified incl. FAIL-CLOSED (model missing→deny, verifier error→deny — never silent allow) + recognized/unrecognized/open; esbuild clean (purple.ts + VoiceEnroll). Verified isolated /tmp. resemblyzer optional ([speaker] extra); gate enforced only once enrolled.
- *** M3 (Deeper "knows me") COMPLETE: 3a smart memory+profile+self-awareness, 3b document RAG, 3c persona, 3d speaker-ID voice gate. ***
- NEXT: M4 real-time duplex voice (streaming STT/TTS, barge-in). Then M5 long-horizon autonomy (planner + background jobs + sub-agents), M6 smart home.

## 2026-06-12T21:20:00Z — Plan: M4 (real-time duplex voice)
- Action: scoped M4 into 4a (streaming reply pipeline: Ollama token stream + pure sentence_chunks + cancelable Kokoro playback queue) and 4b (barge-in via live-mic-during-TTS + VAD, continuous conversation mode until idle timeout, chunked STT, per-utterance speaker gate). Decisions: all 3 priorities (barge-in/streaming/hands-free); continuous session; chunked faster-whisper STT. Appended plan to JARVIS_ROADMAP.md. No code yet.

## 2026-06-12T21:50:00Z — M4 slice 4a: streaming reply pipeline
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md (M4), memory.
- Action: OllamaLLM.stream_chat() async generator (Ollama stream=True → yields content tokens; building block for token→TTS in 4b). voice/streaming.py (NEW): pure flush_sentences(buffer)→(sentences,remainder) + stream_sentences(token_aiter) + StreamingVoice(synth, play) cancelable player (cancel() stops after current step → barge-in groundwork). VoiceLoop._speak_reply: speaks replies sentence-by-sentence via StreamingVoice (long answers start playing at sentence 1, cancelable), falls back to one-shot when stream_voice off; stores self._player for 4b barge-in cancel. config stream_voice(True).
- Files: src/purple/llm/ollama_client.py, src/purple/voice/streaming.py (new), src/purple/voice/loop.py, src/purple/config.py, tests/unit/test_streaming.py (new).
- Result: ruff clean (streaming + wiring excerpts); 6/6 streaming tests pass (flush_sentences boundaries/newline/none, stream_sentences from tokens, StreamingVoice plays-all + cancel-stops-early); wiring excerpts compile. Verified isolated /tmp. (Live mic/audio timing verified on user's PC.)
- NEXT (4b, finishes M4): true token-stream→TTS-as-generated (agent streaming path for final answer); barge-in (live mic during playback → VAD from enrolled speaker → cancel self._player + capture); continuous conversation mode (listen→reply→listen until idle timeout); chunked STT. Speaker gate per utterance.

## 2026-06-12T22:30:00Z — M4 slice 4b: barge-in + conversation mode (M4 COMPLETE)
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md (M4), memory.
- Action: streaming.py +pure should_barge_in(is_speaking,speech_detected) + conversation_continues(transcript,mode). VoiceLoop: _converse(stream) = wake→back-and-forth (listen→reply→listen) until idle (conversation_continues / empty capture); _record_command gains start_timeout_frames (idle end); _run calls _converse after wake. Barge-in: _speak_reply spawns _barge_monitor (own short mic stream; on speech → self._player.cancel() + emit barge_in) gated by enable_barge_in; interrupting utterance still speaker-gated by process_command. config enable_barge_in/conversation_mode/conversation_idle_seconds.
- Files: src/purple/voice/streaming.py, src/purple/voice/loop.py, src/purple/config.py, tests/unit/test_streaming.py.
- Result: ruff clean (streaming + loop excerpt); 8/8 streaming tests pass (chunking + should_barge_in + conversation_continues + StreamingVoice play/cancel); loop excerpt compiles. Live mic/playback timing verified on user's PC.
- ENGINEERING NOTE: for SPOKEN replies, sentence-level streaming (4a) is the right granularity — TTS needs whole sentences, so token-by-token streaming gives no audible benefit; llm.stream_chat remains available for an optional text-UI word-streaming enhancement (not needed for voice). So 4a+4b deliver the spoken-latency + barge-in + hands-free wins the user asked for.
- *** M4 (real-time duplex voice) COMPLETE: streaming sentence playback (4a) + barge-in + continuous conversation mode (4b), all speaker-gated. ***
- NEXT: M5 long-horizon autonomy (planner + background jobs + sub-agents). Then M6 smart home. (Deferred: weekly auto-consolidation; age-based fact decay; optional text-UI token streaming.)

## 2026-06-12T23:00:00Z — Plan: M5 (long-horizon autonomy)
- Action: scoped M5 into 5a (planner + Mission/Step store + sequential executor; Step seam designed for later delegation), 5b (background runner + guardrails: confirm-risky + milestone checkpoints + budget cap; Missions UI), 5c (sub-agents that delegate down into the sequential executor for larger/fan-out steps, recursion w/ depth limit). Decisions: background missions; confirm-risky+checkpoints; sequential-first THEN sub-agents (don't skip; tap into sequential). Appended plan to JARVIS_ROADMAP.md. No code yet.

## 2026-06-12T23:45:00Z — M5 slice 5a: planner + mission store + sequential executor
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md (M5), memory.
- Action: autonomy/plan.py (pure: planning_prompt, parse_plan [JSON or numbered/bulleted, capped], next_pending, mission_outcome [stop-on-failure], status constants). Models Mission + Step(FK CASCADE). autonomy/missions.py: MissionStore (DB ops, SQLAlchemy imported LAZILY per method so the runner is testable without a DB) + MissionRunner (plan_and_create via llm.chat→parse_plan; run_mission = sequential loop, persists after each step, resumable, stops on cancel/failure; _run_step = the SEAM for sub-agent delegation later; deny-risky approver so unattended missions can't auto-confirm high-risk tools). runtime +get_agent/set_agent (app sets it). tools/missions.py: start_mission (plan+create+background asyncio task), mission_status, list_missions, cancel_mission (registered). /missions API (list/get/start/cancel) + app.state.missions task set.
- Files: src/purple/autonomy/{__init__,plan,missions}.py (new), src/purple/memory/models.py, src/purple/runtime.py, src/purple/tools/missions.py (new), src/purple/tools/__init__.py, src/purple/app.py, tests/unit/test_missions.py (new).
- Result: ruff clean (plan/missions/tools-missions/runtime/test); 8/8 tests pass (parse_plan json+numbered+empty, planning_prompt, next_pending, mission_outcome, run_mission happy/stops-on-failure/cancel-midway via fake store+agent); models + missions py_compile OK; missions imports without sqlalchemy (lazy). Verified isolated /tmp.
- NEXT (5b): background runner in the always-on service (resume on restart, pause/resume); guardrails = milestone checkpoints + confirm-risky (per autonomy) + step/budget cap; Missions UI tab. Then 5c sub-agents (delegate into sequential via _run_step). Then M6 smart home.

## 2026-06-13T00:20:00Z — M5 slice 5b: background runner + guardrails + Missions UI
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md (M5), memory.
- Action: plan.py +is_commit_step + needs_checkpoint(title, autonomy) [act → only commit steps (buy/pay/send/book/delete/...); confirm/notify → every step]. missions.py: run_mission(approve_step_id) now CHECKPOINTS — sets BLOCKED + notifies via bus instead of running a checkpoint step; resume(mission_id) approves the currently-blocked step then continues (re-checkpoints at the next). _notify broadcasts to the UI feed; MissionStore.list_running for resume-on-restart. app lifespan _resume_missions() relaunches missions left 'running' after a restart; POST /missions/{id}/resume route; resume_mission tool. UI: 6th tab "missions" (MissionsPanel) — start a goal, live-polling list with status badges, expand steps + results, Approve (blocked), Cancel. purple.ts mission types + missions/mission/start/cancel/resume.
- Files: src/purple/autonomy/{plan,missions}.py, src/purple/app.py, src/purple/tools/missions.py, frontend/ui-src/{purple.ts,App.tsx}, tests/unit/test_missions.py.
- Result: ruff clean (plan/full-missions/test); 12/12 mission tests pass (incl. checkpoint→BLOCKED, resume runs-one-then-blocks, act-runs-until-commit-step, stop-on-failure, cancel-midway); full missions.py compiles; esbuild clean (purple.ts + MissionsPanel). Verified isolated /tmp. Background/DB behaviour PC-verified.
- *** M5 5b done: missions run in background, pause at checkpoints (confirm/act), resumable + resume-on-restart, Missions UI. *** NEXT 5c: sub-agents delegate into the sequential executor via _run_step (depth-limited). Then M6 smart home.

## 2026-06-13T01:00:00Z — M5 slice 5c: sub-agents (delegate into the executor) — M5 COMPLETE
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md (M5), memory.
- Action: plan.py +is_complex_step (>=8 words / "and" / hints like research/compare/each/multiple) + should_delegate(depth, max_depth, complex). missions.py _run_step(depth) now DELEGATES a complex step (when depth < mission_max_depth) to _delegate, which plans the step into sub-steps (agent.llm.chat→parse_plan) and runs each via _run_step(depth+1) — a sub-agent running its own mini-plan through the SAME executor (depth-limited recursion); results merge back as the step result; falls back to inline if <2 sub-steps or planning fails. Sub-steps get a "focused sub-agent" prompt. config mission_max_depth(1). run_mission/resume pass depth=0.
- Files: src/purple/autonomy/{plan,missions}.py, src/purple/config.py, tests/unit/test_missions.py.
- Result: ruff clean (plan/missions-excerpt/test); 14/14 mission tests pass (incl. is_complex/should_delegate, _run_step delegates-complex [plans once, 2 sub-steps run at depth1, no runaway recursion], no-delegate-when-simple, + all 5a/5b). Verified isolated /tmp.
- *** M5 (long-horizon autonomy) COMPLETE: 5a planner+mission store+sequential executor, 5b background runner+checkpoints+resume+Missions UI, 5c sub-agents delegating into the executor (depth-limited). ***
- NEXT: M6 smart home (the last roadmap milestone). DEFERRED backlog: weekly auto-consolidation; age-based fact decay; optional text-UI token streaming; persisted sub-missions (5c runs sub-plans inline within the parent step).

## 2026-06-13T01:40:00Z — M6 (thin): Home Assistant smart-home foundation
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md, memory. User has no smart devices yet → chose a thin, ecosystem-agnostic foundation (won't be wasted: HA fronts most gear, local-first).
- Action: smarthome.py — pure domain_of / parse_states / action_to_service + HAClient (httpx lazy; configured(); list_entities → GET /api/states; call_service → POST /api/services/{domain}/{service}; control(entity,action)) talking to a LOCAL Home Assistant REST API (Bearer long-lived token). config ha_base_url/ha_token; config_api exposes ha_base_url (EDITABLE) + ha_token (SECRET). tools/smarthome.py: list_devices, control_device (on/off/toggle) — both report "not set up" when unconfigured. Registered in load_tools. No new dep (httpx already present).
- Files: src/purple/smarthome.py (new), src/purple/config.py, src/purple/config_api.py, src/purple/tools/smarthome.py (new), src/purple/tools/__init__.py, tests/unit/test_smarthome.py (new).
- Result: ruff clean (smarthome + tool + test); 5/5 pure tests pass (domain_of, parse_states, action_to_service, configured gate, control rejects bad action). Verified isolated /tmp. HONEST: live device control is UNVERIFIABLE until the user runs Home Assistant + a device — pure logic is tested; the wiring is reviewed.
- *** ALL 6 JARVIS ROADMAP MILESTONES NOW HAVE LANDED (M6 in thin/foundation form). *** 
- NEXT / DEFERRED backlog: run Purple end-to-end on the PC (setup→selfcheck→first conversation/mission) to shake out real bugs; full M6 (brightness/scenes/climate services, device watchers→triggers) once HA+devices exist; weekly auto-consolidation; age-based fact decay; optional text-UI token streaming; persisted sub-missions.

## 2026-06-13T02:30:00Z — Backlog quick wins: weekly consolidation, fact decay, chat streaming
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md, memory.
- Action: (1) Weekly auto-consolidation — scheduler.py _memory_hygiene_job (Sun 03:00 cron) calls memory.consolidate() + decay_facts(); config consolidate_weekly(True). (2) Age-based fact decay — extract.is_decayable(category, age_days, max_age_days) pure (only 'fact' decays, off when 0); store.decay_facts() deletes kind='fact' older than memory_decay_days; config memory_decay_days(0=off); run in the weekly job. (3) Text-UI streaming — voice/streaming.word_groups(text,size) pure chunker; /ws now streams the reply as {type:reply_chunk} word-groups then the full {type:reply}; purple.ts ReplyChunkMsg + onReplyChunk; App.tsx streams into a live purple bubble then finalizes (display-streaming — no latency change, just live-typing feel; voice already streams at sentence level).
- Files: src/purple/config.py, src/purple/scheduler.py, src/purple/memory/{extract,store}.py, src/purple/voice/streaming.py, src/purple/app.py, frontend/ui-src/{purple.ts,App.tsx}, tests/unit/{test_memory_extract,test_streaming}.py.
- Result: ruff clean (extract/streaming/scheduler/decay-excerpt); pure helpers verified (is_decayable truth table, word_groups round-trip); decay excerpt + scheduler compile; streaming client TS esbuild clean. Verified isolated /tmp.
- Backlog now: full M6 (needs HA+devices); persisted sub-missions; news/weather in briefing; active-window context. Plus: RUN PURPLE END-TO-END on the PC (still nothing run on hardware).

## 2026-06-13T03:30:00Z — Scheduler catch-up + real briefing (delivered)
- Context read: project CLAUDE.md, JARVIS_ROADMAP.md, memory.
- Action: (1) CATCH-UP — jobs.py (persistent last-run JSON in data_dir; pure is_overdue). scheduler records run per job + on boot runs overdue jobs (_catch_up: briefing if >1d, hygiene if >7d) so a job missed while PC was off runs next boot. (2) BRIEFING FIX — briefing.py now REAL: weather via Open-Meteo (geocode+forecast, no key, from weather_location) + headlines via RSS feeds (parse_rss), REPLACING the LLM-hallucinated headlines; deliver() pushes to UI feed + toast + speak (audio-gated) instead of log-only. config weather_location + news_feeds (default BBC world). config_api exposes weather_location.
- Files: src/purple/jobs.py (new), src/purple/briefing.py (rewritten), src/purple/scheduler.py (rewritten: deliver + record_run + _catch_up), src/purple/config.py, src/purple/config_api.py, tests/unit/test_briefing.py (new).
- Result: ruff clean (briefing/jobs/scheduler/test); pure logic verified (parse_rss order+limit+bad-xml, weather_line incl unknown code, is_overdue never/over/under) via UTC-shim (sandbox py3.10; target py3.12); scheduler compiles + RUF006 fixed (held catch-up task ref). 
- HONESTY FIX recorded: briefing was the one "feature in name" — computed-but-not-delivered + LLM-invented news; now real + delivered.
- NEXT: user runs Purple END-TO-END tonight (setup.ps1 → selfcheck → first conversation/mission). Active-window observe-toggle: discussed, awaiting go.

## 2026-06-17T00:00:00Z — Boot greeting + observe toggle + self-suggestions + run guide
- Context: project CLAUDE.md, JARVIS_ROADMAP, memory. User running Purple tonight; wanted (a) start/stop-observing toggle built, (b) a time-aware boot greeting (weather one-liner + news offer), (c) self-improvement suggestions to come from Purple herself, (d) a copy-paste run guide.
- OBSERVE (privacy, off by default): src/purple/observe.py (state + pure should_auto_off + active_window_title via pygetwindow + context_note injected into agent; auto-off after observe_auto_off_hours; observe_log_history writes observed window to activity log — real effect, not a dead flag). tools/observe.py (set_observing / observation_status tools → "start/stop observing"). orchestrator injects observe.context_note() after selfstate. app.py: /observe GET+POST, observe_default boot wire. config + config_api: observe_default/observe_log_history/observe_auto_off_hours.
- GREETING: src/purple/greeting.py (pure greeting_line: morning=offer help, 12-17 day going, 17-22 how was your day, late = up late; + weather one-liner + "say 'news'" offer + ≤1 self-suggestion). Delivered on boot via app.state.greeting_task (enable_greeting). tools/briefing.py: get_weather / get_news / morning_briefing tools so "yes, news" returns REAL data (not hallucinated). config enable_greeting; config_api exposes it.
- SELF-LEARNING: src/purple/usage.py (durable tool-use log in data_dir; pure summarize_counts; registry.execute records every tool call). src/purple/suggestions.py (pure derive_suggestions: heavy use of escape hatches run_command/browser_open/pc_ui -> "want a dedicated tool?"; + config-gap nudges e.g. set weather city). Surfaced in greeting + /suggestions endpoint. This makes the "build a Spotify tool" insight come from Purple, not the dev.
- FRONTEND: purple.ts getObserve/setObserve; App.tsx header Observe toggle (👁) + toggleObserve + feed line; App.css .observe styles. Greeting/suggestions already arrive via event bus into the Monitor feed.
- RUN GUIDE: RUN_TONIGHT.txt (prereqs → setup.ps1 → .env → selfcheck → start → PowerShell smoke tests → feature-by-feature checklist → optional UI → troubleshooting → stop). .env.example: added weather/greeting/observe keys.
- VERIFY: 7 new pure tests pass (greeting_line, observe.should_auto_off + toggle roundtrip, derive_suggestions threshold/order, usage.summarize_counts window/bad-rows) via reconstructed modules with UTC shim (sandbox py3.10; target py3.12). ruff clean on all 7 new py files (faithful config, NULs stripped past the mount-truncation quirk). Edited files (registry/app/orchestrator/config/config_api/tools.__init__) verified correct via authoritative Read; insertions are trivial pattern-matches. TSX/TS: esbuild parsed past every edit (only failed at mount truncation boundary), purple.ts EOF confirmed intact via Read.
- Files: src/purple/{observe,greeting,usage,suggestions}.py (new), src/purple/tools/{observe,briefing}.py (new), tests/unit/test_observe_greeting.py (new), src/purple/agent/orchestrator.py, src/purple/tools/registry.py, src/purple/tools/__init__.py, src/purple/app.py, src/purple/config.py, src/purple/config_api.py, frontend/ui-src/{App.tsx,purple.ts,App.css}, .env.example, RUN_TONIGHT.txt.
- Result: success (logic verified; lint clean on new code). NEXT: live end-to-end run tonight; report back selfcheck/logs output for live fixes.

## 2026-06-17T01:00:00Z — Greeting refinements (weather morning-only, randomized lines)
- Context: user feedback — weather at 8pm is noise; only give it mornings (or as a heads-up for incoming rough weather); news offer fine any time; randomize greeting lines.
- briefing.py: _fetch_weather() split → _fetch_weather_context() (adds hourly weather_code for next ~6h via Open-Meteo, timezone=auto) returning {line,code,upcoming}; _fetch_weather() now thin wrapper (callers unchanged).
- greeting.py: greeting_line now picks at random from 4 variants per time bucket (morning/afternoon/evening/night), pick injectable for deterministic tests. New pure is_morning / should_show_weather (morning OR severe-now OR severe-soon) / weather_sentence (plain morning report vs "Heads-up — rough weather…"). SEVERE_CODES = heavy rain/snow/freezing/thunderstorm WMO codes. build_greeting uses context + gating; news offer unconditional.
- tests: updated test_observe_greeting.py — greeting by-time (keyword), randomized (first≠last variant), name, should_show_weather (morning/calm-evening/severe-now/severe-soon), weather_sentence phrasing. 8 pure tests pass (UTC-shim sandbox).
- ruff: full greeting.py (145L) + full briefing.py (160L) reconstructed faithfully → "All checks passed!" (mount still truncating these on read; verified via heredoc reconstruction).
- Files: src/purple/greeting.py, src/purple/briefing.py, tests/unit/test_observe_greeting.py, RUN_TONIGHT.txt.
- Result: success. Behavior: weather only mornings or severe heads-ups; news offered anytime; greeting varies each boot.

## 2026-06-17T01:30:00Z — Refresh .gitignore + README for repo
- .gitignore: rewrote to cover secrets (.env*, google_credentials.json, google_token.json, voiceprints.json), private data (data/, logs/, models/, *.onnx, *.bin, *.wav), tooling caches (.pytest_cache/.ruff_cache/.mypy_cache/.coverage), correct Tauri paths (frontend/**/src-tauri/target & gen, .vite), IDE/OS cruft. Keeps .env.example tracked.
- README.md: rewrote to current reality — Kokoro (not Piper), all 6 milestones, full feature list (memory/PC/web/proactivity/greeting+briefing/self-suggestions/observe/voice/missions/email/smart-home), accurate architecture, prereqs incl qwen3-vl, setup.ps1 + manual + optional extras ([dev]/[speaker]/[google]/[xtts]/[observability]), entry points (purple/purple-selfcheck/purple-service), smoke test, full project layout (all current modules), Safety & privacy section, bnac dev workflow, scale path. Points to RUN_TONIGHT.txt.
- Files: .gitignore, README.md. Result: success (docs only).

## 2026-06-17T02:00:00Z — Fix setup.ps1 parse errors (non-ASCII em-dashes)
- Symptom (user, running D:\My-Purple\setup.ps1): cascade of "Missing argument", "string missing terminator", "Missing closing '}'" parse errors.
- Root cause: setup.ps1 + scripts/install_service.ps1 contained em-dash (—, U+2014). Windows PowerShell 5.1 reads .ps1 as ANSI/CP1252, not UTF-8; the UTF-8 em-dash byte 0x94 decodes to a CP1252 curly close-quote, silently opening a string → cascading false brace/terminator errors.
- Fix: rewrote setup.ps1 pure-ASCII (em-dashes → '-'; also removed two stray markdown backticks inside double-quoted strings on the Playwright/Ollama Warn lines). Fixed the one em-dash in scripts/install_service.ps1. uninstall_service.ps1 already clean.
- Verify: no non-ASCII remains in any .ps1; setup.ps1 quotes even (96), braces 27/27, parens 24/24 balanced; only backticks are legit `n newline escapes.
- NOTE for user: their working copy is D:\My-Purple (separate from D:\Purple). Gave in-place one-liner (Get-Content -Raw ... -replace '[^\x00-\x7F]','-' | Set-Content -Encoding ASCII) + copy-from-D:\Purple option.
- Files: setup.ps1, scripts/install_service.ps1. Result: success (verified ASCII + balanced; live PowerShell parse to be confirmed on user's PC).

## 2026-06-18T00:00:00Z — Fix models/data path (src/ root bug) + harden DB setup step
- Symptom (selfcheck on D:\My-Purple): Kokoro WARN looked in <root>\src\models\kokoro but setup.ps1 downloaded to <root>\models\kokoro; PostgreSQL FAIL "password authentication failed for user purple".
- ROOT PATH BUG (real, affects everyone): config.py ROOT = Path(__file__).resolve().parent.parent → after src/ restructure that = <root>/src, so models/, data/, logs/, and config_api's .env all wrongly resolved INSIDE src/. Fixed: ROOT = parents[2] (file is <root>/src/purple/config.py → project root). Now models_dir/data_dir/log_dir + config_api ROOT/.env + google.py ROOT/path all correctly point at the project root (where setup.ps1 + .gitignore + .env already live). Verified path math in sandbox; reviewed all consumers (settings.data_dir/models_dir/log_dir, config_api.ROOT, google.ROOT) — all benefit.
- POSTGRES (environment, not code): setup.ps1 hard-coded PGPASSWORD=postgres and swallowed psql errors (2>$null), so when the user's superuser password differed, role/db/extension were never created. Reworked the DB step to prompt for the superuser password (Enter to skip), CREATE USER purple/'purple' + CREATE DATABASE purple OWNER purple + CREATE EXTENSION vector, and report failures via $LASTEXITCODE instead of hiding them. Pure ASCII.
- User action: D:\My-Purple is a pre-fix copy — copy updated src\purple\config.py (Kokoro path) + setup.ps1; create the DB via psql as superuser (or Docker pgvector/pgvector:pg16). pgvector binary must be installed in their Postgres for CREATE EXTENSION to succeed.
- Files: src/purple/config.py, setup.ps1. Result: success (path verified; live DB on user's PC).

## 2026-06-18T00:30:00Z — setup.ps1 DB step: stop hardcoding the role password
- User flagged: setup.ps1 still hardcoded role password 'purple' while their .env uses 'MyPurple' (mismatch).
- Why it was 'purple': zero-config default matching .env.example's default DSN (purple:purple@...).
- Fix: DB step now prompts for the desired 'purple' role password (Enter = 'purple'), CREATE USER + ALTER USER to force it (converges on re-run), CREATE DATABASE OWNER purple, CREATE EXTENSION vector, then rewrites PURPLE_PG_DSN in .env via regex to embed that password (single source of truth, no drift). CREATE statements wrapped in try{}catch{} so 'already exists' is harmless on re-run.
- Verified real file complete + balanced via Read (mount served a truncated copy → ignore the bash brace-count). Pure ASCII.
- Files: setup.ps1. Result: success. User's manual MyPurple setup already works; this is for future/clean re-runs.
