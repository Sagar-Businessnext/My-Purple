# Memory Index

> Auto-maintained by `bnac-memory-curator`. One line per memory.
> Read by every agent on session start per `~/.claude/rules/context-first.md` (step 5).
> Append entries with `/bnac-memory add <type> "<content>"`. Never edit this file by hand.

---

## User

_(no entries yet — entries appear here when `/bnac-memory add user "..."` is run)_

## Feedback

_(no entries yet — entries appear here when `/bnac-memory add feedback "..."` is run)_

## Project

_(no entries yet — entries appear here when `/bnac-memory add project "..."` is run)_

## Reference

_(no entries yet — entries appear here when `/bnac-memory add reference "..."` is run)_
